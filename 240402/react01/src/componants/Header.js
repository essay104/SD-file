const handOnClick = () => {
  const handlOnClick
};

export const Header = () => {
  return (
    <header>
      <h1>header</h1>
      <button onClick={handlOnClick}>클릭하세요</button>
    </header>
  );
};

export default Header;
