import React from "react";

const Buttons = () => {
  const handlOnClick = (e) => {
    
  };
  return (
    <div>
      <button name="A버튼">A버튼</button>
      <button name="B버튼">B버튼</button>
    </div>
  );
};

export default Buttons;
