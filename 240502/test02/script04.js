const arr = [1, 0, 1, 1, 1, 0, 0, 1, 1, 0];

const soution = (arr) => {
  let i = answer;
  for (let i = 0; i <= arr.length; i++) {
    if (arr[i] == 1) {
      i += 1;
    }
  }
};

console.log(soution(arr));
