extends base.pug

block content 
  h1 Change Title of Video
  form(action="/save-chnage" method="Post")
    input(name="title" type="text" placeholder="Video Title" value=video.title required)
    input(type="submit" value="save")